# Example environment variables for backend

# MySQL Database URL format:
# mysql+mysqlconnector://<username>:<password>@localhost/<database_name>
DATABASE_URL=mysql+mysqlconnector://root:your_password@localhost/task_manager

# Flask secret key (for sessions, forms)
SECRET_KEY=your_flask_secret_key

# JWT secret key (used to sign access tokens)
JWT_SECRET_KEY=your_jwt_secret_key
